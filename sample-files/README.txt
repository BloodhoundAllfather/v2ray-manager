All these files are just samples. You can edit them however you like.

"config" 		is sample application configuration file that should be next to the script
"list" 			is the list-file that the script stores all the information about the users
"config.json"	  	is a sample v2ray configuration file that's at /usr/local/etc/v2ray/config.json

